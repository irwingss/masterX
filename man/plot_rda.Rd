% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_rda.R
\name{plot_rda}
\alias{plot_rda}
\title{Graficar Análisis de Ordenación (RDA, CCA, dbRDA) con Opciones Avanzadas}
\usage{
plot_rda(
  object,
  titulo = NULL,
  scaling = "sites",
  lc = FALSE,
  sitios_nombres = FALSE,
  segmentos_Yvar = FALSE,
  segmentos_Xvar = FALSE,
  sitios = c("point"),
  repel = FALSE,
  color_Yvar = "#fc4903",
  color_Xvar = "#9500e6",
  color_sitios = "#03a9fc",
  alpha_segments_Yvar = 0.5,
  alpha_segments_Xvar = 0.5,
  alpha_sitios = 0.5,
  size_sitios = 3,
  size_Yvar = 3,
  size_Xvar = 3
)
}
\arguments{
\item{object}{Objeto resultante de `vegan::rda`, `vegan::cca`, `vegan::capscale`, `vegan::metaMDS`, `vegan::wcmdscale`, etc.}

\item{titulo}{(Opcional) Título del gráfico.}

\item{scaling}{Tipo de escalado a utilizar. Depende del análisis (por ejemplo, "sites" para RDA/CCA).}

\item{lc}{(Opcional) Lógico. Si `TRUE`, utiliza scores de "lc" (contraposición lineal); si `FALSE`, utiliza scores de "wa" (ponderados). Por defecto `FALSE`.}

\item{sitios_nombres}{(Opcional) Lógico. Si `TRUE`, añade etiquetas a los sitios. Por defecto `FALSE`.}

\item{segmentos_Yvar}{(Opcional) Lógico. Si `TRUE`, dibuja segmentos para las variables explicativas (ejes Y). Por defecto `FALSE`.}

\item{segmentos_Xvar}{(Opcional) Lógico. Si `TRUE`, dibuja segmentos para las variables predictoras (ejes X). Por defecto `FALSE`.}

\item{sitios}{(Opcional) Vector con "point", "text", o ambos. Controla la representación de los sitios. Por defecto `c("point")`.}

\item{repel}{(Opcional) Lógico. Si `TRUE`, usa `geom_text_repel` para las etiquetas de texto. Requiere el paquete `ggrepel`. Por defecto `FALSE`.}

\item{color_Yvar}{(Opcional) Color para las variables explicativas (ejes Y). Por defecto `"#fc4903"`.}

\item{color_Xvar}{(Opcional) Color para las variables predictoras (ejes X). Por defecto `"#9500e6"`.}

\item{color_sitios}{(Opcional) Color para los puntos de los sitios. Por defecto `"#03a9fc"`.}

\item{alpha_segments_Yvar}{(Opcional) Transparencia para los segmentos de variables Y. Por defecto `0.5`.}

\item{alpha_segments_Xvar}{(Opcional) Transparencia para los segmentos de variables X. Por defecto `0.5`.}

\item{alpha_sitios}{(Opcional) Transparencia para los puntos de los sitios. Por defecto `0.5`.}

\item{size_sitios}{(Opcional) Tamaño de los puntos de los sitios. Por defecto `3`.}

\item{size_Yvar}{(Opcional) Tamaño de las etiquetas de las variables Y. Por defecto `3`.}

\item{size_Xvar}{(Opcional) Tamaño de las etiquetas de las variables X. Por defecto `3`.}
}
\value{
Un objeto `ggplot2` con el gráfico de ordenación.
}
\description{
Esta función permite visualizar objetos resultantes de análisis de ordenación como RDA, CCA, dbRDA, NMDS, PCoA, entre otros.
Detecta automáticamente el tipo de análisis y ajusta la visualización en consecuencia.
}
\examples{
\dontrun{
library(vegan)
data(dune)
data(dune.env)

# Ejemplo con RDA
rda_obj <- rda(dune ~ ., data = dune.env)
plot_ordination(rda_obj, titulo = "RDA de Dune", sitios_nombres = TRUE, segmentos_Yvar = TRUE)

# Ejemplo con NMDS
nmds_obj <- metaMDS(dune)
plot_ordination(nmds_obj, titulo = "NMDS de Dune", sitios = c("point", "text"), repel = TRUE)
}

}
