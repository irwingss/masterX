% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_ordination.R
\name{plot_ordination}
\alias{plot_ordination}
\title{Graficar Análisis de Ordenación con Opciones Avanzadas}
\usage{
plot_ordination(
  object,
  titulo = NULL,
  scaling = "sites",
  lc = FALSE,
  sitios_nombres = "point",
  segmentos_Yvar = TRUE,
  overlay_type = "none",
  meta_data = NULL,
  group_col = NULL,
  numeric_col = NULL,
  breaks = c(-Inf, 100, 200, Inf),
  labels = c("<100", "100-200", ">200"),
  repel_sitios = FALSE,
  repel_var = FALSE,
  color_Yvar = "black",
  color_Xvar = "red",
  color_sitios = "blue",
  alpha_segments_Yvar = 0,
  alpha_segments_Xvar = 1,
  alpha_sitios = 1,
  size_sitios = 1.5,
  size_Yvar = 2.5,
  size_Xvar = 2.5
)
}
\arguments{
\item{object}{Objeto resultante de `vegan::rda`, `vegan::cca` o `vegan::capscale`.}

\item{titulo}{(Opcional) Título del gráfico.}

\item{scaling}{Tipo de escalado a utilizar ("sites", "species", etc.). Por defecto "sites".}

\item{lc}{(Opcional) Lógico. Si `TRUE`, utiliza scores de "lc" (linear combination); si `FALSE`, utiliza scores de "wa" (weighted averages). Por defecto `FALSE`.}

\item{sitios_nombres}{(Opcional) Vector de cadenas. Puede contener "point" para añadir puntos y/o "text" para añadir etiquetas a los sitios. Por defecto `FALSE`.}

\item{segmentos_Yvar}{(Opcional) Lógico. Si `TRUE`, dibuja segmentos para las variables explicativas (Yvar, columnas de la matriz Y). Por defecto `FALSE`.}

\item{overlay_type}{(Opcional) Tipo de capa a añadir sobre los puntos:
- "none": Ninguna capa adicional.
- "ellipses": Añade elipses por grupo.
- "density": Añade densidad 2D por grupo.
Por defecto "none".}

\item{meta_data}{(Opcional) Data frame con información adicional de las muestras. Debe tener el mismo número y orden de filas que las muestras en `object`.}

\item{group_col}{(Opcional) Nombre de la columna en `meta_data` que ya es categórica y se usará para agrupar.}

\item{numeric_col}{(Opcional) Nombre de la columna numérica en `meta_data` para crear categorías.}

\item{breaks}{(Opcional) Vector numérico de puntos de corte para `cut()`. Por defecto `c(-Inf, 100, 200, Inf)`.}

\item{labels}{(Opcional) Vector de etiquetas para las categorías. Por defecto `c("<100","100-200",">200")`.}

\item{repel_sitios}{(Opcional) Lógico. Si `TRUE`, usa `geom_text_repel` para las etiquetas de texto de los sitios. Requiere el paquete `ggrepel`. Por defecto `FALSE`.}

\item{repel_var}{(Opcional) Lógico. Si `TRUE`, usa `geom_text_repel` para las etiquetas de texto de las variables (Yvar y Xvar). Requiere el paquete `ggrepel`. Por defecto `FALSE`.}

\item{color_Yvar}{(Opcional) Color para las variables explicativas (Yvar). Por defecto `"#fc4903"`.}

\item{color_Xvar}{(Opcional) Color para las variables predictoras (Xvar). Por defecto `"#9500e6"`.}

\item{color_sitios}{(Opcional) Color para los puntos de los sitios. Por defecto `"#03a9fc"`.}

\item{alpha_segments_Yvar}{(Opcional) Transparencia para los segmentos de Yvar. Por defecto `0.5`.}

\item{alpha_segments_Xvar}{(Opcional) Transparencia para los segmentos de Xvar. Por defecto `0.5`.}

\item{alpha_sitios}{(Opcional) Transparencia para los puntos de los sitios. Por defecto `0.5`.}

\item{size_sitios}{(Opcional) Tamaño de los puntos de los sitios. Por defecto `3`.}

\item{size_Yvar}{(Opcional) Tamaño de las etiquetas de Yvar. Por defecto `3`.}

\item{size_Xvar}{(Opcional) Tamaño de las etiquetas de Xvar. Por defecto `3`.}
}
\value{
Un objeto `ggplot2` con el gráfico de ordenación.
}
\description{
Esta función permite visualizar objetos resultantes de análisis de ordenación como RDA (Redundancy Analysis),
CCA (Canonical Correspondence Analysis) y db-RDA (Distance-based RDA). Ofrece opciones para personalizar la
visualización de sitios, variables explicativas y predictoras, así como la adición de capas adicionales
como elipses y densidad 2D.
}
\examples{
\dontrun{
library(vegan)
library(ggrepel)
data(dune)
data(dune.env)

# Análisis RDA
rda_obj <- rda(dune ~ ., data = dune.env)

# Análisis CCA
cca_obj <- cca(dune ~ ., data = dune.env)

# Análisis db-RDA (capscale)
db_rda_obj <- capscale(dune ~ ., data = dune.env, distance = "bray")

# Graficar RDA con etiquetas de sitios y segmentos para Yvar
plot_ordination(rda_obj, titulo = "RDA de Dune", sitios_nombres = c("point", "text"), segmentos_Yvar = TRUE)

# Graficar CCA con overlay de elipses
plot_ordination(cca_obj, titulo = "CCA de Dune", overlay_type = "ellipses", meta_data = dune.env, group_col = "Management")

# Graficar db-RDA con etiquetas repelentes para sitios y variables
plot_ordination(db_rda_obj, titulo = "db-RDA de Dune", sitios_nombres = c("point", "text"),
                segmentos_Yvar = TRUE, repel_sitios = TRUE, repel_var = TRUE)
}

}
